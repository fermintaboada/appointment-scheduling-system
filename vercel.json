{
  "buildCommand": "cd front/vite-project && npm install && npm run build",
  "outputDirectory": "front/vite-project/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
